---
alwaysApply: true
---
### 3. Последовательность событий и блокировка переходов

1. **Инициализация актора**
   - Отправляется патч snapshot (`add`, `/`) через CustomEvent.
   - Далее отправляется патч состояния (`add`, `/state`) через BroadcastChannel — если переход сопровождается действием, это блокировка переходов (process=true).
2. **Выполнение действия (action) при переходе**
   - Пока выполняется действие, переходы заблокированы (process=true).
   - После завершения действия отправляется патч состояния (`replace`, `/state`) через BroadcastChannel — разблокировка переходов (process=false).
   - Если переход без действия, сразу отправляется патч состояния (`replace`, `/state`).
3. **Изменение контекста**
   - Отправляется патч контекста (`replace`, `/context`) через CustomEvent.
4. **Автопереходы**
   - После каждого патча snapshot и обновления контекста автоматически проверяются условия переходов (autotransitions).
   - Если условия выполнены, происходит смена состояния и отправка соответствующих патчей (с действием или без).

---

### 4. Важные особенности

- **CustomEvent** — только наружу (от актора к родителю), не внутрь closed Shadow DOM.
- **BroadcastChannel** — глобальная синхронизация состояний.
- **Патчи** логируются и тестируются через фикстуры сообщений (RAW MESSAGES, снапшоты).

---

### 5. Пример жизненного цикла патчей

```mermaid
graph TD
    A[Инициализация актора] --> B[CustomEvent: add / (snapshot)]
    B --> C[BroadcastChannel: add /state (блокировка, если есть action)]
    C --> D[CustomEvent: replace /context]
    D --> E[BroadcastChannel: replace /state (разблокировка)]
    E --> F[CustomEvent: replace /context]
    F --> G[...]
```

---

### 6. Реакции и обработка патчей

Реакции в MetaFor используют объектный формат с именованными ключами:

```javascript
.reactions({
  "обработка патчей состояния": {
    filter: ({meta, patch}) => 
      meta.tag === "target-actor" && 
      patch.path === "/state" && 
      patch.value === "active",
    action: ({update, patch}) => {
      update({targetActive: true, lastUpdate: Date.now()})
    }
  },
  "обработка патчей контекста": {
    filter: ({meta, patch}) => 
      meta.tag === "data-source" && 
      patch.path === "/context",
    action: ({update, patch}) => {
      update({data: patch.value, received: true})
    }
  }
})
```

**Особенности реакций:**

- **Именованные ключи** — каждое название реакции является ключом объекта
- **Фильтрация** — `filter` функция определяет, когда срабатывает реакция
- **Действие** — `action` функция выполняется при срабатывании фильтра
- **Блокировка** — опциональный `block: true` предотвращает выполнение других реакций

---

### 7. Рекомендации

- Для передачи данных "вверх" используйте CustomEvent (всплытие).
- Для глобальной синхронизации состояний используйте BroadcastChannel.
- Используйте именованные реакции для лучшей читаемости и отладки.
- Группируйте связанные реакции в одном объекте для логической организации.

- Для передачи данных "вверх" используйте CustomEvent (всплытие).
- Для глобальной синхронизации состояний используйте BroadcastChannel.
